PROJECT(beam++)
ENABLE_LANGUAGE(CXX)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET(CMAKE_CXX_FLAGS "-fPIC -g -rdynamic")
set(BEAM_SRCS
beam.cpp
EulerBernoulliBeam.cpp
main.cpp
)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
ADD_EXECUTABLE(beam++ ${BEAM_SRCS})
